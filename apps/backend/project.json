{
  "name": "backend",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run uvicorn app.main:app --reload --port 8000",
        "cwd": "apps/backend"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo backend lint placeholder"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo backend test placeholder"
      }
    }
  }
}
